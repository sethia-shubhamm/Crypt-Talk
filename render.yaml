# Render.com deployment configuration for Crypt-Talk

# Build Command
npm install --prefix public && npm run build --prefix public && pip install -r server/requirements.txt

# Start Command
cd server && python app.py

# Environment
PYTHON_VERSION=3.11.5
NODE_VERSION=18.17.0